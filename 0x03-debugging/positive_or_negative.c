#include "main.h"
/**
 * positive_or_negative - show pos or neg
 * @i: the number 0
 *main - Fnx here
 *Descrbtion: Out puts negative or positive
 *Return: always 0
 */
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}
